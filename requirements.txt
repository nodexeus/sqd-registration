web3>=7.0,<8
base58>=2.1
# Pinned to a major version: names are derived deterministically from the peer
# ID, so a wordlist change would rename peers that are not yet registered.
coolname>=5.0,<6
python-dotenv>=1.0
pytest>=8.0
